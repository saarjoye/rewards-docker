# 资源、依赖与权限

- 运行时要求 Node.js 24；源码位于 `src/`，构建命令为 `npm run build`，静态检查为 `npm run lint`。
- 现有离线测试位于 `scripts/tests/`，先构建再用 Node 直接运行。不得使用真实账号或启动真实奖励任务验证 parser。
- 可直接进行最小源码读取、构建、只读检查和已有离线测试。
- 安装或升级依赖、启动 Docker/浏览器/长期服务、修改系统配置、数据迁移及外部账号操作必须先确认。
- `npm run pre-build` 会安装依赖和浏览器，不属于常规验证命令，未经确认不得运行。
