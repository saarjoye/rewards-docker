# 资源、依赖与权限

- 运行时为 Node.js 24；后端使用 TypeScript ESM、Fastify、内置 SQLite，浏览器使用固定版本 Patchright/Chromium。
- 可直接读取非敏感源码、规则、清单和测试，可运行已有离线检查。
- 安装或升级依赖、安装浏览器、启动 Docker/长期服务、执行真实账号操作和修改系统网络配置必须先确认。
- 不运行会自动安装依赖或浏览器的隐式脚本。缺少依赖时只做不依赖安装的结构与语法检查，并报告限制。
- 命令失败最多重试三次，每次必须有新证据；禁止扩大权限掩盖失败。
