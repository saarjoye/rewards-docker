# Git 管理

- 任务开始检查仓库根、分支、工作树和 remote 元数据，但不自动 fetch、pull、push 或创建 PR。
- 保留用户既有修改；只处理本任务文件，不使用 `git add .`、`git reset --hard`、`git clean` 或强制覆盖。
- 只有用户明确授权才提交。提交前列出文件范围并运行 `git diff --check`、构建、相关测试和敏感信息扫描。
- `dist/` 是构建产物，不作为源码修复入口；真实配置、会话、日志和诊断文件永不提交。
